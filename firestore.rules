rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Portfolio content (projects, experience, etc.) is public to read.
    // All writes happen from the Firebase Console / Admin SDK, never from the site.
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
