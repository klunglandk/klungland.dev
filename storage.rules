rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Portfolio images/assets are public to read via their download-token URLs.
    // All uploads happen from the Firebase Console, never from the site.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
